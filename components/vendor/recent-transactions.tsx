'use client';

import { useState } from 'react';
import { Eye, PackageCheck, X } from 'lucide-react';
import { StatusBadge } from '@/components/ui/badge';

export interface RecentItem { id: string; product_name: string; quantity: number; line_total: number; fulfil_status: string; outlet_name: string; outlet_id?: string; outlet_location?: string }
export interface RecentOrder { order_id: string; display_id?: string | null; created_at: string; product_name: string; outlet_name: string; outlet_id?: string; outlet_location?: string; order_total: number; quantity: number; item_count: number; fulfil_status: string; order_status: string; items: RecentItem[] }

function dateLabel(value: string) {
  return new Date(value).toLocaleDateString('en-MY', { day: '2-digit', month: 'short', year: 'numeric' });
}

export default function RecentTransactions({ items }: { items: RecentOrder[] }) {
  const [selected, setSelected] = useState<RecentOrder | null>(null);

  return <>
    <div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-gray-100"><tr className="text-gray-500"><th className="px-6 py-4 font-medium">Order & date</th><th className="px-6 py-4 font-medium">Items</th><th className="px-6 py-4 font-medium">Outlet</th><th className="px-6 py-4 text-right font-medium">Vendor total</th><th className="px-6 py-4 font-medium">Status</th><th className="px-6 py-4 text-right font-medium">Action</th></tr></thead><tbody className="divide-y divide-gray-50">{items.map((item) => <tr key={item.order_id} className="group hover:bg-gray-50/60"><td className="px-6 py-4"><div className="font-mono text-xs font-semibold text-gray-900">{item.display_id || `#${item.order_id.slice(0, 8)}`}</div><div className="mt-1 text-xs text-gray-500">{dateLabel(item.created_at)}</div></td><td className="px-6 py-4"><div className="flex items-center gap-2"><PackageCheck size={17} className="text-emerald-700" /><span className="font-medium text-gray-900">{item.product_name}</span></div><div className="mt-1 text-xs text-gray-500">Qty: {item.quantity} · {item.item_count} line{item.item_count === 1 ? '' : 's'}</div></td><td className="px-6 py-4"><p className="font-medium text-gray-700">{item.outlet_name}</p><p className="mt-1 text-xs text-gray-400">{item.outlet_location} · ID {item.outlet_id?.slice(0, 8).toUpperCase()}</p></td><td className="px-6 py-4 text-right font-semibold text-gray-900">RM {Number(item.order_total).toFixed(2)}</td><td className="px-6 py-4"><StatusBadge status={item.fulfil_status} /></td><td className="px-6 py-4 text-right"><button type="button" onClick={() => setSelected(item)} className="inline-flex items-center gap-1 rounded-lg bg-emerald-50 px-3 py-2 text-xs font-semibold text-emerald-800 hover:bg-emerald-100"><Eye size={14} /> View</button></td></tr>)}{!items.length && <tr><td colSpan={6} className="px-6 py-14 text-center text-gray-400">No transactions found in this period.</td></tr>}</tbody></table></div>
    {selected && <div className="fixed inset-0 z-40 bg-gray-950/20" onClick={() => setSelected(null)}><aside onClick={(event) => event.stopPropagation()} className="absolute right-0 top-0 h-full w-full max-w-md overflow-y-auto bg-white p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-emerald-700">Recent order</p><h2 className="mt-1 text-xl font-bold text-gray-950">{selected.display_id || `#${selected.order_id.slice(0, 8)}`}</h2></div><button type="button" onClick={() => setSelected(null)} className="rounded-lg p-2 text-gray-400 hover:bg-gray-100"><X size={18} /></button></div><div className="mt-6 space-y-3">{selected.items.map((item) => <div key={item.id} className="rounded-xl border border-gray-100 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-gray-900">{item.quantity}× {item.product_name}</p><p className="mt-1 text-xs text-gray-500">{item.outlet_name}</p><p className="mt-1 font-mono text-[10px] text-gray-400">{item.outlet_location} · ID {item.outlet_id?.slice(0, 8).toUpperCase()}</p></div><p className="font-semibold text-gray-900">RM {Number(item.line_total).toFixed(2)}</p></div><div className="mt-3"><StatusBadge status={item.fulfil_status} /></div></div>)}</div><div className="mt-6 rounded-xl bg-gray-50 p-4"><p className="text-xs text-gray-500">Vendor total</p><p className="mt-1 text-2xl font-bold text-gray-950">RM {Number(selected.order_total).toFixed(2)}</p><p className="mt-1 text-xs text-gray-500">Order status: {selected.order_status}</p></div></aside></div>}
  </>;
}
